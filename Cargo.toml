[package]
name = "semantic-release"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
git2 = "0.14"
clap = {version = "3.1.18", default-features = false, features = ["std", "cargo"] }
regex = "1.5.6"
lazy_static = "1.4.0"
